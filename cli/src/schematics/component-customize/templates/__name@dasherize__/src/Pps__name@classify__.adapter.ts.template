import { CustomizeComponentAdapter } from '@/core/dtos/component-customize.dto'

export interface Pps<%= classify(name) %>Adapter extends CustomizeComponentAdapter {
  on?: {}
}

export interface Pps<%= classify(name) %>Output {
  default: string
}
